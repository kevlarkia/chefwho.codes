# Session Resume (opening prompt)

Copy everything below the line into a new chat. Replace the placeholder with the Save State Markdown from the prior session.

---

We are resuming an ongoing project. Below is a Save State checkpoint from the prior session. Ingest it, adopt the role stated in §1 (do not invent a persona), and prepare to continue.

**[INSERT COPIED CHECKPOINT TEXT HERE]**

**Directives:**
1. Analyze the checkpoint for goal, progress, decisions, and next steps.
2. If you can see live context (repo, branch, working tree, open PR, docs), reconcile it against the checkpoint and list any **conflicts or staleness** before confirming.
3. Conflict policy: live repo / `AGENTS.md` / HELD decisions win over a stale checkpoint; call out the mismatch.
4. Do **not** begin the actual work yet.
5. Reply with a concise confirmation that you have assimilated the context (and any conflicts).
6. End exactly by asking: `Systems synchronized. Are we ready to begin [first item from §5]?` and wait for my command.
