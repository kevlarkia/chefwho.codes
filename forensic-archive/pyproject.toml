[project]
name = "forensic-archive"
version = "0.1.0"
description = "Dual-pass LLM archive pipeline with an immutable SQLite provenance ledger."
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
